import React from 'react'
import { AuthOTP } from '@pages/auth/otp-code'
import { OTPCodeScreenStackProps } from '../../types'

export const OTPCodeScreen: React.FC<OTPCodeScreenStackProps> = () => {
    return (
        <AuthOTP />
    )
}
