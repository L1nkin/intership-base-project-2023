import React from 'react'
import { AuthPasswordWriting } from '@pages/auth/password-writing'
import { PasswordScreenStackProps } from '../../types'

export const PasswordScreen: React.FC<PasswordScreenStackProps> = () => {
    return (
        <AuthPasswordWriting />
    )
}
